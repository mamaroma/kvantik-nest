<header class="site-header" role="banner">
  <div class="logo-block">
    <a href="/<%= session?.isAuthed ? ('?auth=1&user=' + encodeURIComponent(session.user)) : '' %>" aria-label="Квантик — на главную">
      <img src="/images/logo.jpg" alt="Логотип журнала «Квантик»" />
    </a>
    <h1>Квантик — научно-популярный журнал</h1>
  </div>

  <nav class="main-nav" aria-label="Основная навигация">
    <%- include('./menu', { activePage, session }) %>
  </nav>

  <%- include('./session', { activePage, session }) %>
</header>
